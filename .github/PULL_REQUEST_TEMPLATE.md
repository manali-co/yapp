## What

## Why

## How it was tested

## Checks
- [ ] one feature per branch, branched from `dev`
- [ ] ruff, mypy, pytest green locally
- [ ] threshold changes include the `yapp eval` / `yapp tasks` table
- [ ] UI changes were made in Claude Design first
- [ ] no new shell call outside the `run` seam; guard changes come with a test
