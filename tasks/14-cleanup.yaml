name: 14-cleanup
# Yapp launches TextEdit, then is told to clean up: TextEdit must be gone again.
instruction: open text edit and then clean up
placement: hand_over
setup:
  - pkill -x TextEdit
check:
  - app_not_running: TextEdit
settle_seconds: 3
